include({{ os.path.join(os.getenv("HOME"), "opt/gcc14/x-tools/x86_64-bionic-linux-gnu.profile.conan") }})

[settings]
compiler.cppstd=gnu23

[buildenv]
MKLROOT={{ os.path.join(os.getenv("HOME"), "intel/oneapi/mkl/latest")}}

[conf]
tools.cmake.cmaketoolchain:generator=Ninja
